var countBits = function(n) {
  let binaryArray = n.toString(2).split("")
  let count = 0
  for(i = binaryArray.length; i >= 0; i--){
    if (binaryArray[i] == 1){
      count++
    }
  }
  return count
};
