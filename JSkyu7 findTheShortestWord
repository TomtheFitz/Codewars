function findShort(s){
  let arr = s.split(" ")
  let shortestString = 99999
  for(i = 0; i < arr.length; i++){
    if (arr[i].length < shortestString){
      shortestString = arr[i].length
    }
  }
  return shortestString
}
